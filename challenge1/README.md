# Print Practice

## 📌 Description
This program prints a simple recipe to the console using Python `print()` statements. The output must match the example exactly, including punctuation, casing, and spacing.

---

## ✅ Requirements
- Python 3.x installed on your system.
- No external libraries are required.

---

## 🛠 How to Run
1. Copy the code below into a file named `recipe.py`:

```python
1. Mix 500g of Flour, 10g Yeast and 300ml Water in a bowl.
2. Knead the dough for 10 minutes.
3. Add 3g of Salt.
4. Leave to rise for 2 hours.
5. Bake at 200 degrees C for 30 minutes.
```

2. Open a terminal or command prompt.
3. Navigate to the folder where `recipe.py` is saved.
4. Run the program using:

```bash
python recipe.py
```

---

## 📤 Example Output
The output should look exactly like the image provided in **`example_output.jpg`**:

*(Refer to `example_output.jpg` for visual confirmation.)*

---

## ✅ Notes
- Ensure punctuation and casing match the example exactly.
